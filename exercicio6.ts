let largura: number = 30 
let comprimento: number = 60

let calcArea = function(lagA: number , comB: number): number{
    return lagA * comB
}
 console.log(`Área é: ${calcArea(largura,comprimento)} m²`)