export let sobra = function(x1: number) : boolean{
    if (x1 %2 == 0){
       return true
    }
    else{
        return false
    }
}

    export let calcArea = function(lagA: number , comB: number): number{
        return lagA * comB
    }

    export let calcubo = (numero: number) => {
        return Math.pow (numero, 3)
    }
